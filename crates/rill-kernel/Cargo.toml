[package]
name = "rill-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PTY owner, sole writer, bounded byte ring (kernel lane)"

[features]
# Negative-control mutations (ADR 0002 D3). Only `validate-spike0.sh
# --negative-controls` enables this; shipping builds carry no mutation code.
mutate = []

[dependencies]
rill-attach.workspace = true
libc.workspace = true
