# Byte compile artifacts
/target
/.build
*.o
.DS_Store
# Xcode
xcuserdata/
DerivedData/
# Secrets
.env
*.pem
# Vendored emulator (fetched, not committed)
/third_party/ghostty
# Packaged app
/dist
*.app
# Gate evidence artifacts (ADR 0002 D10). Generated per run; PRs attach the
# artifact rather than committing it. Commit one deliberately only when it is
# the record that closes a gate.
/evidence
# cargo-fuzz scratch (separate workspace under crates/rill-attach/fuzz)
/crates/rill-attach/fuzz/target
/crates/rill-attach/fuzz/Cargo.lock
